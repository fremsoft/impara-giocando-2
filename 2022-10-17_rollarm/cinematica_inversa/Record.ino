//Read the values ot the potentiometers.
void readPot()
{
  potenziometro[0] = analogRead(A0);
  potenziometro[1] = analogRead(A1);
  potenziometro[2] = analogRead(A2);
  potenziometro[3] = analogRead(A3);
}
